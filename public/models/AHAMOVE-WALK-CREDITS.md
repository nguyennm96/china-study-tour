# Ahamove walking mascot

Source supplied by the user: `outputs/ahamove-walk/ahamove-walk.glb`.
Copied without modifying the asset. See `outputs/ahamove-walk/README.md` for the
model's construction and branding references. The original mascot was modeled
for this project; it is not a downloaded stock character.

The live map uses the default `Walk` scene, its one-second `Walk` animation,
and the original orange/navy materials. Forward is +Z, up is +Y; one walk cycle
travels 0.4096 model units. Movement on the map is illustrative, not a real route.
